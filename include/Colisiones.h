#pragma once
#include "Constantes.h"
#include <vector>
#include <string>

using namespace std;

// Declaración externa del mapa
extern const vector<string> mapa;

// Declaración de la función (sin definición)
bool sePuedeMover(int x, int y);
